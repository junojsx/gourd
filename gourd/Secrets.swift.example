//
//  Secrets.swift.example
//  gourd
//
//  This file is a template. To set up the project:
//  1. Copy this file and rename the copy to "Secrets.swift"
//  2. Fill in your real values (find them at supabase.com → Project Settings → API)
//  3. Add the new Secrets.swift to the Xcode target if it isn't already
//  4. Never commit Secrets.swift — it's in .gitignore
//

enum Secrets {
    // MARK: - Supabase
    static let supabaseURL     = "https://YOUR_PROJECT_ID.supabase.co"
    static let supabaseAnonKey = "YOUR_ANON_KEY_HERE"
}
